<div id="ans-id-<%= answer.id %>">
    <h3><%= answer.content %></h3>
    <%= link_to "Eliminar", [@question, answer], method: 'delete', remote: true, data: { confirm: "¿Are you sure you want to delete this question?" } %> 
    <%= render answer.comments.order(created_at: :asc) %>
    <%= render "comments/form", commentable: answer %>
    
</div>