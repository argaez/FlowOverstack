<%= form_for [commentable, @question.comments.new], remote: true do |f| %>
    <div class="form-group">
        <% if user_signed_in? %>
            <%= f.text_area :content, rows: 2, placeholder:"Escribe tu comentario aquí...", class:"form-control", id:"com-box" %>
        <% end %>
    </div>
    <%= f.submit 'Comentar', class:"btn btn-outline-dark float-right"%>
<% end %>